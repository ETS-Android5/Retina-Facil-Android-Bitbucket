<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unichristus.lit.retinafacil"
    android:versionCode="3"
    android:versionName="1.2">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Views.SplashActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/SplashTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Views.MainActivity">

        </activity>

        <activity android:name=".Views.LoginActivity">

        </activity>

        <activity android:name=".Views.PatientActivity">

        </activity>

        <activity android:name=".Views.GalleryActivity">

        </activity>

        <activity android:name=".Views.QuizActivity">

        </activity>

        <activity android:name=".Views.ResultActivity">

        </activity>

        <activity android:name=".Views.LaminasActivity">

        </activity>

        <activity android:name=".Views.RetinopatiaActivity">

        </activity>

        <activity android:name=".Views.AboutActivity">

        </activity>

        <activity android:name=".Views.ConceitosActivity">

        </activity>
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.unichristus.lit.retinafacil.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths"
                tools:replace="android:resource"/>
        </provider>
    </application>


</manifest>